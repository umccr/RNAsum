# Session Information Kable

Session information kables for vignettes.

## Usage

``` r
session_info_kable(pkgs = NULL)
```

## Arguments

- pkgs:

  Vector of R packages to display in the vignette. By default returns
  all.

## Value

A list with two kables containing information about the platform and the
specified packages.
