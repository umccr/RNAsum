#!/usr/bin/env Rscript

# Dependencies that must be explicity in the project in 
# order for renv to download them
suppressMessages(library(MASS))
suppressMessages(library(mgcv))
suppressMessages(library(rpart))
